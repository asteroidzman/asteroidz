# Options deliberately absent from docs/, one per line, as:
#
#     option_key: why
#
# The reason is mandatory. An escape hatch nobody has to justify stops being one,
# which is the same rule tests/schema-exempt.txt runs on.
#
# Before adding anything here, check which of the two lists check-config-docs.py
# put the option in. It only FAILS on options that appear nowhere in docs/ at
# all. An option shown in a ```kdl example but never named as a term is reported
# as a note and does not need an entry -- backticking its name where it is
# already explained is a one-word fix and leaves the docs better than an
# exemption would.
#
# Empty on purpose: all 95 options are documented. If that is still true when you
# read this, the honest change is usually to write the doc.
